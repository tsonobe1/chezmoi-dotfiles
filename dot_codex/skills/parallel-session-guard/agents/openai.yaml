interface:
  display_name: "Parallel Session Guard"
  short_description: "Inspect active Codex work before starting parallel sessions."
  default_prompt: "Review current Codex sessions, worktrees, and changed files, then propose a conflict-safe work area or a queued follow-up for the owning session."
